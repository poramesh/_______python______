Input Format

The first line contains an integer .

Constraints
1<=n <=150

Output Format

Print the list of integers from 1 through n  as a string, without spaces.


Sample Input 0

3
Sample Output 0

123

......
n = int(input())

s = ""
for i in range(1,n+1):
    s += str(i)
(or)   
print(s)
for i in range(1,n+1): 
    print(i,end='')
#In python cursor will go to next line automatically so using end=" " we can stop the cursor
    
(or)
result = ''.join(str(i) for i in range(1, n + 1))
print(result)
(or)
print(*range(1, int(input())+1), sep='')

For short operator '*' allows you to pass an array. Example:

user = [name, password, captcha]

log_in(*user)

The * is actually used to unpack a iterator. *[1,2,3] mean 1 2 3

In python cursor will go to next line automatically so using end=" " we can stop the cursor
